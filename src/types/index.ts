export interface Track {
  id: string
  title: string
  artist: string
  duration: string
  genre: string
}

export interface Video {
  id: string
  title: string
  channel: string
  videoId: string
}
